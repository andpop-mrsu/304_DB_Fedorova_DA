# Лабораторная работа 1. Подготовка окружения для выполнения заданий и интерактивной работы с СУБД SQLite
## Описание файлов

### Исходные данные
- **ratings.csv** — файл с оценками пользователей фильмов. Содержит следующие колонки:
  - `userId` — идентификатор пользователя
  - `movieId` — идентификатор фильма
  - `rating` — оценка (от 0.5 до 5.0)
  - `timestamp` — временная метка оценки
- **movies.csv** — файл с информацией о фильмах. Содержит колонки:
  - `movieId` — идентификатор фильма
  - `title` — название фильма
  - `genres` — жанры фильма
- **tags.csv** — файл с тегами, которые пользователи присваивали фильмам. Содержит колонки:
  - `userId` — идентификатор пользователя
  - `movieId` — идентификатор фильма
  - `tag` — текстовый тег
  - `timestamp` — временная метка
- **users.txt** — файл с информацией о пользователях
- **genres.txt** — список жанров фильмов
- **occupation.txt** — список профессий пользователей

### Результаты выполнения
- **ratings_count.txt** — файл с результатами анализа:
  - Строка 1: минимальный userId и количество его оценок
  - Строка 2: максимальный userId и количество его оценок

- **sqlite.txt** — информация об установленной СУБД SQLite:
  - Версия SQLite
  - Доступные режимы вывода данных

## Выполненные действия
1. Настроено окружение Git и GitHub
2. Создана ветка Task01
3. Проанализирован файл ratings.csv
4. Установлена и протестирована СУБД SQLite